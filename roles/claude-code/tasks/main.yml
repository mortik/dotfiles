---
- name: create claude config directory
  file:
    path: "{{ ansible_env.HOME }}/.claude"
    state: directory

- name: setup claude-code settings
  copy:
    src: "settings.json"
    dest: "{{ ansible_env.HOME }}/.claude/settings.json"
