[defaults]
inventory = ./hosts
interpreter_python = auto_silent
# Becomes the default in ansible-core 2.24; opted in early so any use of a
# top-level fact (ansible_env) instead of ansible_facts['env'] fails loudly.
inject_facts_as_vars = False
